# 工资管理系统 设计需求文档

> 版本：v0.1（初稿）  
> 日期：2026-07-31  
> 数据来源：`D:\java_project\salary_system\工资明细表.xlsx`

> **进展更新（2026-07-31）**：数据库已确定为 Oracle（TRMUSR），表结构与历史数据已完成初始化：
> 已在 `TRMUSR` 库创建 5 张 `SLR_` 前缀表，并成功导入 45 条月度工资记录、62 条批注、205 个公式（含缓存值），经校验数据一致。
>
> **系统已实现（2026-07-31 晚）**：Spring Boot 3 + Vue 3 前后端分离系统已完成 M1~M5 全部里程碑并交付：
> 登录认证（ADMIN/MANAGER/VIEWER 三角色）、工资记录增删改查与月度详情、批注管理、Excel 数据导入（增量/替换）、
> 统计分析与导出、用户管理与操作日志。单端口部署：`start-backend.bat` 后访问 http://localhost:8080（默认 admin/admin123）。

---

## 1. 项目概述

### 1.1 背景

当前工资信息以 Excel 文件（`工资明细表.xlsx`）维护，包含 `工资`、`个税数据`、`试算系数` 三个 sheet。现有方式的痛点：

1. **历史数据查询不便**：跨年月对比、年度汇总需要手工筛选和计算。
2. **批注信息容易丢失**：工资表中有大量批注（奖金构成、补发说明等），散落在单元格上，不便于检索、统计和长期留存。
3. **公式与数据混存**：存在公式、试算区（第 48 行）、以及 2026 年 7-9 月"考核"列缺失等数据质量问题，不利于结构化存储。
4. **敏感数据缺乏保护**：工资为个人敏感信息，Excel 明文存放且无访问控制与操作审计。

### 1.2 目标

建设一个轻量的 Web 工资管理系统，实现：

- 将历史工资数据从 Excel 导入并结构化管理；
- **随时查询历史工资**，支持按月份、年份、字段、批注关键词等多维度检索与对比；
- **批注作为一等数据管理**：导入时完整保留 Excel 批注，系统内可查看、编辑、新增、检索批注，并可将批注中的奖金明细结构化；
- 提供年度汇总、趋势分析等基础统计能力；
- 保证数据安全与可追溯（权限控制、操作审计、数据备份）。

### 1.3 范围

**一期（核心）**：

- Excel 数据导入（`工资` sheet 为主，含批注解析）；
- 工资记录管理（查询 / 详情 / 编辑 / 删除）；
- 批注管理（保留、展示、检索、新增、编辑）；
- 基础统计（年度到手、月度实发趋势、年度对比）；
- 数据导出（Excel / CSV）；
- 用户登录与权限、操作日志。

**二期（扩展，可选）**：

- `个税数据`、`试算系数` 两个 sheet 的导入与在线试算；
- 批注明细自动结构化（如"饭贴+高低温：750"拆分为 名称+金额）；
- 工资变动提醒、批注待办提醒；
- 移动端适配 / 小程序。

---

## 2. 用户与角色

| 角色 | 说明 | 核心权限 |
| --- | --- | --- |
| 管理员 | 系统配置、用户管理、数据导入/导出 | 全部权限 |
| 数据维护人 | 工资数据与批注的日常维护 | 查询、编辑工资记录、管理批注、导出 |
| 普通查看者（可选） | 仅查看 | 只读查询与导出 |

> 建议初期单机/单人使用：管理员即数据维护人，权限模型预留扩展即可。

---

## 3. 现状数据盘点（重要输入）

### 3.1 `工资` sheet

- 数据范围：`A1:AF46`，共 32 列。
- 月度记录：第 2~46 行，共 **45 条**，覆盖 **2023-01 ~ 2026-09**。
- 第 48 行为试算区（`E48`、`G48` 有公式），导入时应**排除**。
- 表头分四组：
  - **A~L：应发部分**（含"加项合计"）；
  - **M~T：代扣部分**（含"扣项合计"与"实发金额"）；
  - **U~AB：公司缴纳部分**（含"公司合计"）；
  - **AC~AF：其他奖金 / 年度奖金**（单独发放口径）。

#### 字段明细（导入目标结构）

| 列 | 字段名 | 类型 | 说明 / 示例 |
| --- | --- | --- | --- |
| A | 考核等级 | 文本 | `B1`、`A1`、`A2`、`A3`；2026-07~09 缺失 |
| B | 月份 | 日期(年月) | 2023-01 ~ 2026-09 |
| C | 岗位工资 | 金额 | 1958 ~ 2233（随晋升调整） |
| D | 地区补贴 | 金额 | 1174.8 ~ 1339.8 |
| E | 奖金 | 金额 | 月度奖金，随等级变化 |
| F | 其他 | 金额 | 年中奖、赋能精英、流程医生奖金等（有批注） |
| G | 岗位津贴 | 金额 | |
| H | 加班工资 | 金额 | |
| I | 购房补贴 | 金额 | |
| J | 工作补贴 | 金额 | |
| K | 其他/补差 | 金额 | 未休假补薪、行级补差、工资补差（有批注） |
| L | 加项合计 | 金额(公式) | `=SUM(C:K)` |
| M | 养老保险(个人) | 金额 | 基数随社保基数调整（如 1284.8 → 1799.04） |
| N | 失业保险(个人) | 金额 | |
| O | 医疗保险(个人) | 金额 | |
| P | 住房公积金(个人) | 金额 | |
| Q | 企业年金(个人) | 金额 | 2025 年起有值 |
| R | 个人所得税 | 金额 | |
| S | 扣项合计 | 金额(公式) | `=SUM(M:R)` |
| T | 实发金额 | 金额(公式) | `=L-S` |
| U | 养老保险(公司) | 金额 | |
| V | 失业保险(公司) | 金额 | |
| W | 医疗保险(公司) | 金额 | |
| X | 工伤保险(公司) | 金额 | |
| Y | 生育保险(公司) | 金额 | |
| Z | 住房公积金(公司) | 金额 | |
| AA | 企业年金(公司) | 金额 | |
| AB | 公司合计 | 金额(公式) | `=SUM(U:AA)` |
| AC | 其他奖金（单独发放） | 金额 | 多数为公式（如 `=750+2100`），批注解释构成 |
| AD | 年度奖金 | 金额 | 12 月/1 月发放 |
| AE | 个人所得税(年度奖金) | 金额 | |
| AF | 实发金额(年度奖金) | 金额 | 部分为公式 |

#### 批注现状

- 共 **62 条**批注，作者均为 `wei61`，**无时间戳**（Excel 批注未记录创建时间）。
- 分布规律：
  - `F` 列：各类奖金说明（年中奖金、赋能精英、流程医生奖金、AI+业务团队赛等）；
  - `K` 列：未休假补薪、行级补差、工资补差；
  - `AC` 列：其他奖金构成明细（饭贴+高低温、一次性高低温、开门红包、过节费、卡庆、读书费、行庆等）；
  - `AD/AF` 列：年度/长期奖金说明（如"长期服务奖金：11246.65""两笔：9090+56419.58"）。
- 批注内容多为 `名称：金额` 逐行结构（以换行分隔），**适合结构化解析**。

### 3.2 `个税数据` sheet（二期）

- 2019~2026 年度汇总：年度到手、个税 APP 金额、到手/个税增降幅、社保基数、月均、公司缴纳各项。
- 适合做"年度总收入趋势"和"社保基数变化"分析。

### 3.3 `试算系数` sheet（二期）

- 考核等级（A1/A2/A3/B1/B2/C）对应系数、档位（第一~第六档）、总池子、人数。
- 属于薪资测算逻辑，可作为二期"在线试算"的数据源。

---

## 4. 功能需求

### 4.1 数据导入

**FR-01 上传与解析**

- 支持上传 `.xlsx` 文件，选择目标 sheet（默认 `工资`）。
- 自动识别表头并映射字段；表头缺失或列不匹配时给出错误提示。
- 读取公式的**计算缓存值**（如 `L2`、`T2`），同时保留公式文本（如 `=SUM(C2:K2)`）与奖金构成表达式（如 `=750+2100`）。

**FR-02 批注解析**

- 导入时完整提取每个单元格的批注（作者、内容、所属单元格坐标）。
- 将批注挂接到 **月份 + 字段** 维度（如：2023-01 / AC / "饭贴+高低温：750…"）。
- 可选（一期可仅保留原文，二期做结构化）：按 `名称：金额` 规则拆分批注为明细条目。

**FR-03 校验与预览**

- 导入前预览：展示解析后的记录条数、批注条数、警告项（如考核列缺失、负数奖金、公式无法取值）。
- 重复月份记录：提示"覆盖 / 跳过 / 保留为新版本"三种策略。
- 校验规则：月份唯一、金额为数值、实发金额与加项-扣项一致性（差异超过阈值时告警）。

**FR-04 导入批次管理**

- 每次导入生成一个批次记录（时间、文件名、导入人、成功/失败数），支持回滚或追溯某条记录的来源批次。

### 4.2 工资记录管理

**FR-05 查询**

- 默认按月份倒序列表展示月度工资。
- 支持组合筛选：年份、月份区间、考核等级、字段值范围（如实发金额区间）、是否有批注、批注关键词。
- 结果列可配置显隐，支持排序。

**FR-06 详情**

- 月度详情页按四组分区展示：应发 / 代扣 / 公司缴纳 / 其他奖金与年度奖金。
- 每个有批注的字段显示批注标记（如角标），点击展开批注内容。
- 展示合计与实发金额，并显示公式来源（如"加项合计 = SUM(C:K)"）。

**FR-07 编辑**

- 允许手工新增 / 编辑 / 删除月度记录（金额、考核等级等）。
- 合计类字段由系统自动重算，禁止直接编辑；若手工录入与公式口径不一致，需确认提示。
- 所有变更记录操作日志（操作人、时间、修改前后值）。

### 4.3 批注管理（核心关注点）

**FR-08 批注查询**

- 批注列表页：按月份、字段、关键词（如"高低温""未休假""流程医生"）检索。
- 点击批注可跳转到对应月度记录的字段详情。

**FR-09 批注编辑**

- 支持新增、修改、删除批注；每条批注记录：内容、作者、创建时间、更新时间、所属月份+字段。
- Excel 导入的批注若带作者（如 wei61），保留作者；**系统内新增的批注自动记录当前用户与时间**。

**FR-10 批注结构化（二期）**

- 将 `名称：金额` 形式的批注拆为结构化明细，支持按奖金类型汇总（如统计历年"过节费"总额）。
- 对无法解析的文本保留原文并标记"未解析"。

### 4.4 统计分析

**FR-11 年度汇总**

- 按年汇总：加项合计、扣项合计、实发金额、公司合计、其他奖金、年度奖金；与上一年对比（金额与增幅）。

**FR-12 趋势图**

- 月度实发金额趋势、年度到手对比、社保基数变化、公司缴纳合计趋势。

**FR-13 奖金构成分析（依赖批注结构化，二期）**

- 按奖金类型（过节费、高低温、饭贴、开门红包等）聚合统计。

### 4.5 导出

**FR-14 导出**

- 查询结果可导出为 `.xlsx` / `.csv`。
- 导出内容包含批注列（月份+字段+批注内容），保证"批注不丢"。

### 4.6 系统管理

**FR-15 用户与权限**（多用户时启用）

- 登录认证；角色权限控制（见第 2 节）。

**FR-16 操作审计**

- 记录登录、导入、增删改、导出等关键操作日志。

**FR-17 数据备份**

- 支持数据库定时备份 / 一键导出全量 JSON 备份（含批注）。

---

## 5. 数据模型设计

### 5.1 核心表：工资记录表 `salary_record`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint PK | 主键 |
| month | date | 月份（唯一） |
| grade | varchar(10) | 考核等级（A1/A2/A3/B1…） |
| post_salary | decimal(12,2) | 岗位工资 |
| region_allowance | decimal(12,2) | 地区补贴 |
| bonus | decimal(12,2) | 奖金 |
| other_income | decimal(12,2) | 其他 |
| post_subsidy | decimal(12,2) | 岗位津贴 |
| overtime_pay | decimal(12,2) | 加班工资 |
| housing_subsidy | decimal(12,2) | 购房补贴 |
| work_allowance | decimal(12,2) | 工作补贴 |
| other_adjust | decimal(12,2) | 其他/补差 |
| total_income | decimal(12,2) | 加项合计（系统重算） |
| pension_personal | decimal(12,2) | 养老保险(个人) |
| unemployment_personal | decimal(12,2) | 失业保险(个人) |
| medical_personal | decimal(12,2) | 医疗保险(个人) |
| housing_fund_personal | decimal(12,2) | 住房公积金(个人) |
| annuity_personal | decimal(12,2) | 企业年金(个人) |
| income_tax | decimal(12,2) | 个人所得税 |
| total_deduction | decimal(12,2) | 扣项合计（系统重算） |
| net_pay | decimal(12,2) | 实发金额（系统重算） |
| pension_company | decimal(12,2) | 养老保险(公司) |
| unemployment_company | decimal(12,2) | 失业保险(公司) |
| medical_company | decimal(12,2) | 医疗保险(公司) |
| injury_company | decimal(12,2) | 工伤保险(公司) |
| maternity_company | decimal(12,2) | 生育保险(公司) |
| housing_fund_company | decimal(12,2) | 住房公积金(公司) |
| annuity_company | decimal(12,2) | 企业年金(公司) |
| company_total | decimal(12,2) | 公司合计（系统重算） |
| other_bonus | decimal(12,2) | 其他奖金（单独发放） |
| annual_bonus | decimal(12,2) | 年度奖金 |
| annual_bonus_tax | decimal(12,2) | 个税(年度奖金) |
| annual_bonus_net | decimal(12,2) | 实发(年度奖金) |
| formula_json | text | 保留 Excel 公式（含奖金构成表达式） |
| source_batch_id | bigint | 来源导入批次 |
| created_at / updated_at | datetime | 创建 / 更新时间 |

### 5.2 批注表 `salary_comment`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint PK | 主键 |
| record_id | bigint FK | 关联工资记录 |
| field_code | varchar(20) | 字段标识（如 `F`、`K`、`AC`、`AD`） |
| author | varchar(50) | 批注作者（导入时为 wei61） |
| content | text | 批注原文 |
| items_json | json | 结构化明细（二期：名称+金额列表） |
| created_at / updated_at | datetime | 时间戳（导入的旧批注以导入时间为准） |

> 说明：同一单元格可能有多条批注历史，可增加 `version` 支持批注版本历史（二期）。

### 5.3 辅助表

- `import_batch`：导入批次（文件名、时间、操作人、结果统计）。
- `operation_log`：操作日志（操作人、动作、对象、前后值、时间）。
- `user` / `role`：用户与角色（多用户时）。
- `annual_tax_summary`（二期）：个税数据年度汇总。
- `salary_coefficient`（二期）：试算系数。

---

## 6. 页面与交互设计

### 6.1 页面清单

| 页面 | 说明 |
| --- | --- |
| 登录页 | 账号密码登录（单用户可默认跳过或固定账号） |
| 总览仪表盘 | 最近月份实发、年度到手、年度对比、批注数量等卡片；实发金额趋势图 |
| 工资记录列表 | 按月倒序，筛选、排序、列显隐、行内跳转详情 |
| 月度详情 | 四组分区展示；批注角标与展开；编辑入口 |
| 批注管理 | 批注列表、关键词检索、新增/编辑/删除 |
| 数据导入 | 上传 → 解析 → 校验预览 → 执行导入 → 结果反馈 |
| 统计分析 | 年度汇总表、趋势图、同比环比（二期含奖金构成） |
| 系统设置 | 用户、角色、备份、导入批次管理 |

### 6.2 关键交互

- **批注展示**：详情页字段旁显示 `[批注]` 标记，点击弹出气泡/抽屉显示完整批注；批注页检索结果点击直达对应月份详情。
- **导入向导**：四步（上传 → 映射确认 → 校验预览 → 导入结果），校验警告逐条列出，可"仅导入合法记录"或"取消"。
- **金额编辑**：录入原始项后合计实时重算；合计项灰显不可编辑。
- **趋势图**：折线图展示月度实发，可切换按年；柱状图展示年度到手与增幅。

---

## 7. 非功能需求

| 类别 | 要求 |
| --- | --- |
| 数据安全 | 工资属敏感个人信息：数据库加密存储（如字段级加密/应用层加密）、传输走 HTTPS、导出文件需审计 |
| 权限控制 | 最小权限原则；查看者不可导出（按需） |
| 性能 | 45 条记录规模很小，任何查询 < 1s；预留 10 年×12 条 = 120 条/人的量级即可 |
| 可用性 | 界面响应式；导入解析错误有明确提示；操作有确认与撤销（软删除） |
| 可维护性 | 表结构、字段映射可配置；批注解析规则可扩展 |
| 兼容性 | 支持 Excel 2016+ 生成的 `.xlsx`（含公式缓存值） |
| 部署 | 初期单机部署（本机/内网），支持 Docker 打包（二期） |

---

## 8. 技术方案建议

当前目录为 `D:\java_project\salary_system`，按 Java 项目规划。

### 方案 A（推荐）：前后端分离

- 后端：Java 17 + Spring Boot 3 + MyBatis-Plus + **Oracle（已确认：TRMUSR 库）**
- 前端：Vue 3 + Vite + Element Plus + ECharts
- 数据库：**Oracle Database 23ai Free**，连接 `jdbc:oracle:thin:@wei6130.top:1521/TRMUSR`
- 导入解析：Apache POI（读取公式缓存值：`FormulaEvaluator` / `data_only` 模式；批注：`Cell.getCellComment()`）
- 构建：Maven

### 方案 B（更轻）：单体全栈

- Spring Boot + Thymeleaf + Bootstrap + ECharts，单工程、无独立前端，适合纯本机使用。

### 数据库现状（重要）

- `TRMUSR` 库是**在用的业务库**（现有 144 张表），工资系统所有对象统一使用 **`SLR_` 前缀**，与业务表隔离。
- 已建表：`SLR_IMPORT_BATCH`（导入批次）、`SLR_SALARY_RECORD`（工资记录）、`SLR_SALARY_COMMENT`（批注）、`SLR_OPERATION_LOG`（操作日志）、`SLR_APP_USER`（用户）。
- 已导入：45 条记录（2023-01 ~ 2026-09）、62 条批注、205 个公式（缓存值 + 公式文本，存于 `FORMULA_JSON`）。
- 数据库脚本与工具位于项目 `db/` 目录：`schema.sql`（DDL）、`InitSchema.java`（建表）、`ImportExcel.java`（导入，支持 `replace` 重导）、`VerifyImport.java`（校验）、`ConnTest.java`（连通性检查）。

### 关键实现要点

1. **公式值读取**：用 `WorkbookFactory` 以 `data_only=true` 读取缓存值；需要公式文本时另以 `data_only=false` 打开比对。
2. **批注读取**：`cell.getCellComment().getText()` 与 `.getAuthor()`；批注坐标（列号）→ 字段映射。
3. **表头映射**：以表头中文名为准做字段映射表，容忍列顺序变化。
4. **金额精度**：统一 `Decimal(12,2)`，避免浮点误差。

---

## 9. 里程碑

| 阶段 | 内容 | 预计工作量 |
| --- | --- | --- |
| M1 数据层 | 建表、导入解析（含批注）、校验 | ✅ 已完成（2026-07-31） |
| M2 查询与详情 | 记录列表、筛选、月度详情、批注展示 | 2 天 |
| M3 批注管理 | 批注增删改查、关键词检索 | 1~2 天 |
| M4 统计与导出 | 年度汇总、趋势图、Excel/CSV 导出 | 2 天 |
| M5 系统完善 | 登录权限、操作日志、备份、打磨 | 2 天 |

---

## 10. 待确认事项

1. **使用人数**：仅本人使用，还是多人（是否需要完整登录与权限）？
2. **部署方式**：本机运行即可，还是需要部署到服务器供多设备访问？
3. **技术栈偏好**：是否接受方案 A（Vue 前端），还是希望纯 Java 单体（Thymeleaf）？
4. **个税数据与试算系数**：二期功能本期是否需要纳入？
5. **批注时间**：历史批注无时间戳，是否接受以"导入时间"作为创建时间？
