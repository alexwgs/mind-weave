# MindWeave · 织脑 - 设计文档

> 在现有系统（React + Semi Design / Spring Boot + Oracle / 微信小程序）基础上扩展为个人综合系统「MindWeave · 织脑」：编织我的第二大脑与 AI 智能体网络。
> 三个客户端（桌面 Web、移动 Web、微信小程序）共用同一后端，数据实时同步。

## 1. 总体架构

```
┌─────────────┬──────────────┬──────────────┐
│ 桌面/移动 Web │  微信小程序    │  Bark 推送     │
│ (React+Semi)│  (原生小程序)  │  (iOS 设备)   │
└──────┬──────┴──────┬───────┴──────┬───────┘
       │  HTTPS 同域  │  HTTPS 8445  │  HTTP(S)
       └──────────────┼─────────────┘
                      ▼
            Spring Boot 后端（新增 toolkit 模块）
                      ▼
            Oracle TRMUSR（SLR_ 工资 + TK_ 工具库）
                      ▼
            本地存储 /vol1/1000/dev/uploads（附件，加密文件名）
```

- 现有工资模块（SLR_*）保持不变；
- 新增工具库模块统一使用 `TK_` 前缀；
- 所有凭证密码使用 **AES-256-GCM 加密**后入库，密钥来自环境变量 `SALARY_VAULT_KEY`，服务端按需解密，前端不接触主密钥；
- 定时任务（Spring `@Scheduled`）负责 Bark 提醒、每日汇总、逾期催办、循环任务生成。

## 2. 模块一：我的保险箱（密码/凭证管理）

### 数据模型

| 表 | 说明 |
| --- | --- |
| TK_VAULT_GROUP | 自定义分组（社交媒体/金融银行/开发运维/WiFi 网络等） |
| TK_VAULT_ITEM | 凭证条目：名称、账号、密码(密文)、备注、上次查看/修改时间 |
| TK_VAULT_FIELD | 动态自定义字段（键值对，如 AppID/Secret） |
| TK_ATTACHMENT | 附件（图片/小文件，<5MB，加密文件名），BIZ_TYPE=VAULT |

### 功能

- 分组管理：增删改查、排序；
- 凭证 CRUD：名称（必填）、账号、密码、备注 + 动态键值对字段；
- 密码加密存储（AES-256-GCM，服务端密钥），接口默认返回脱敏值；
- 智能生成密码：大小写字母 + 数字 + 特殊字符，随机 16~20 位，一键填入/复制；
- Web 端“小眼睛”点击后明文显示 **5 秒**自动隐藏；“一键复制账号/密码”独立按钮；
- 小程序端默认全部掩码（`****`），需验证 **PIN 码**（首次在设置中创建）后 10 分钟内可查看明文；
- 附件上传：图片/小文件，文件名随机化存储；
- 日志：记录“上次查看时间”“上次修改时间”。

## 3. 模块二：文章管理系统（知识库/CMS）

### 数据模型

| 表 | 说明 |
| --- | --- |
| TK_ARTICLE_CATEGORY | 多级分类（parent_id 树形，如 技术/后端/Java） |
| TK_ARTICLE | 文章：标题/摘要/标签/状态(草稿|发布)/Markdown 正文/分享参数 |
| TK_ATTACHMENT | 附件（图片/视频/文件），BIZ_TYPE=ARTICLE，可按文章关联查看 |

### 功能

- 富文本/Markdown 编辑器：Markdown 语法编辑 + 实时预览 + 图片/视频插入；
- 图片上传（粘贴/选择），正文按 `![说明](attachment:ID)` 引用，渲染时解析为 `/api/tool/attachments/{id}/content`；
- 视频上传 MP4，正文用 `@video(attachment:ID)` 引用，渲染为 HTML5 `<video>`；大视频建议服务端转 H.264（本期提供上传与播放，转码列为后续增强）；
- 分类（多级树）+ 自由标签（逗号分隔）；
- 状态：草稿（仅自己可见）与发布（可分享）；
- 分享：生成外链 `/share/{token}`，支持有效期（默认 7 天）与访问密码；
- 附件管理子菜单：按文章关联查看全部附件。

## 4. 模块三：日程管理与智能提醒（To-Do + Bark）

### 数据模型

| 表 | 说明 |
| --- | --- |
| TK_TODO | 待办：标题/截止时间/优先级/项目/提醒时间/循环规则/推送标记 |
| TK_TODO_SUB | 子任务（Checklist） |
| TK_SETTING | 系统设置（Bark URL、每日汇总时间、PIN 哈希） |

### 功能

- 视图：列表视图 + 日历视图（月历点击日期查看当日待办）；
- 待办属性：标题、截止时间、优先级（高/中/低）、项目标签、完成状态；
- 子任务：一条待办下多个子步骤；
- Bark 推送（服务端每分钟扫描）：
  - 定时提醒：`REMIND_TIME` 到达即推送；
  - 逾期催办：截止时间已过且未完成，首次逾期推送一次；
  - 每日汇总：每日固定时间推送“明日待办预告”；
  - 循环任务：`RECUR_RULE`（如 `WEEKLY:MON:10:00`）到期自动生成下一周期副本；
- 设置页：Bark URL（含设备 Key）、每日汇总时间、“发送测试推送”按钮、PIN 码设置。

## 5. API 一览（均需 JWT，除标注外）

| 模块 | 接口 |
| --- | --- |
| 保险箱 | `GET/POST/PUT/DELETE /api/tool/vault/groups`、`/api/tool/vault/items`、`/api/tool/vault/items/{id}/reveal`、`PUT /api/tool/vault/pin`、`POST /api/tool/vault/pin/verify` |
| 附件 | `POST /api/tool/attachments`（multipart）、`GET /api/tool/attachments/{id}/download`、`GET /api/tool/attachments/{id}/content`、`GET /api/tool/attachments`、`DELETE /api/tool/attachments/{id}` |
| 文章 | `GET/POST/PUT/DELETE /api/tool/articles`、分类 `GET/POST/PUT/DELETE /api/tool/categories`、`POST /api/tool/articles/{id}/share` |
| 分享 | `GET /api/tool/share/{token}`（公开，无 JWT，支持密码） |
| 待办 | `GET/POST/PUT/DELETE /api/tool/todos`、子任务 `POST/PUT/DELETE /api/tool/todos/{id}/subs`、`GET /api/tool/todos/calendar?month=` |
| 设置 | `GET/PUT /api/tool/settings`、`POST /api/tool/settings/bark-test` |

## 6. 安全说明

- 凭证密码 AES-256-GCM 加密（密钥 `SALARY_VAULT_KEY`，生产环境用环境变量注入）；
- 附件文件名随机化（UUID），不落原始文件名；
- PIN 码 BCrypt 哈希存储，仅用于小程序端解锁查看；
- 分享链接含随机 token，可设有效期与访问密码；
- 所有接口按当前登录用户（owner）隔离。

## 7. 部署

- 后端：新增 `SALARY_VAULT_KEY` 环境变量；`UPLOAD_DIR` 默认 `/vol1/1000/dev/uploads`；
- Web：构建后上传 dist；
- 小程序：新增页面，重新上传审核；
- 数据库：执行 `db/toolkit-schema.sql`（同一 TRMUSR 库）。

## 8. 精细权限模型（RBAC）

在原有角色（ADMIN/MANAGER/USER）基础上新增三层控制：

1. **菜单权限**：每个菜单项对应一个权限码（dashboard/records/comments/import/stats/users/logs/vault/articles/todos/settings），无权限则不显示菜单；
2. **操作权限**：新增/编辑/删除/导出等按钮级权限（如 records.create / records.edit / records.delete / records.export / import.execute 等），后端服务层同时校验（`PermissionService.require`），即使绕过前端也会被拒绝；
3. **数据访问范围**：用户可设置 `ALL`（全部）/ `GRADES`（仅指定级别，逗号分隔）/ `NONE`（无），工资记录列表、详情、统计接口自动按级别过滤。

数据表：`SLR_PERMISSION`（权限字典）、`SLR_ROLE_PERMISSION`（角色默认权限）、`SLR_USER_PERMISSION`（用户级覆盖 ALLOW/DENY）。
用户管理页可对每个账号做三态覆盖（默认/允许/禁止），并配置数据范围。执行 `db/permission-schema.sql` 后由后端启动时自动写入种子数据（34 个权限码、3 个角色默认集）。

## 9. 公开文章展示板块

- 新增公开接口：`GET /api/tool/articles/public`（仅已发布文章分页列表）、`GET /api/tool/articles/public/{id}`（公开详情，自动累计浏览数），无需登录；
- Web 端新增 `/blog` 文章展示页（卡片列表 + 搜索 + 详情阅读），登录页提供入口；后台“知识库”继续负责发文（草稿/发布/分类/标签/附件）；
- 小程序端“知识库”新增“我的文章 / 公开文章”切换。

### 富文本编辑

- 编辑器支持**富文本 / Markdown 双模式**：富文本基于 wangEditor（粘贴截图、上传图片、上传 MP4 视频自动入库），
  Markdown 模式保留原语法编辑与实时预览；
- 富文本与 Markdown 可互相转换（turndown），保存时同时落库 `content_html` 与 `content_md`；
- 阅读端（文章详情/展示板块/分享页）优先渲染富文本 HTML，小程序端自动下载受保护附件图片后展示。

## 10. 系统设置扩展

- Bark 推送：推送地址、每日汇总时间、启用/停用开关、发送测试推送；
- 站点设置：站点名称（顶部导航与登录页展示）、公告、页脚文字（存于 `TK_SETTING`，owner=system）；
- PIN 码设置（小程序查看保险箱明文用）。

## 11. 桌面端菜单与权限交互

- 菜单分组：工资相关功能统一收纳到 **收支管理** 子菜单（工资记录 / 批注管理 / 数据导入 / 统计分析），
  子项仍按权限过滤，整组无权限时自动隐藏；
- 用户管理权限配置升级为**树状结构**：模块（菜单）为一级节点，新增/编辑/删除/导出等操作为二级节点，
  每个节点独立设置 默认✓ / 允许 / 禁止；
- 设置页修复：`Input.TextArea` 按 Semi 版本规范改为独立 `TextArea` 引入，并增加全局错误边界，
  页面出错时直接显示错误信息而不是白屏。
