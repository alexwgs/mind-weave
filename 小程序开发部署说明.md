# MindWeave · 织脑 - 微信小程序（原生开发）

小程序与 Web 端共用同一套生产后端（`https://wei6130.top:8445`），数据实时同步。
当前使用**账号密码登录**（系统管理员分配的用户名/密码），无需改动后端即可使用。

已内置个人综合工具库模块：

- **首页**：工资概览 + 功能快捷入口；
- **保险箱**：凭证分组/增删改、动态字段、密码生成、附件、PIN 码解锁查看明文；
- **知识库**：文章列表/新建、分类筛选、Markdown 阅读、分享链接复制；
- **待办**：未完成/已完成筛选、优先级、截止/提醒时间、循环规则、子任务；
- **我的-设置**：Bark 推送地址、每日汇总时间、PIN 码设置。

> 说明：文章富文本编辑、视频上传等重操作建议在 Web 端完成；小程序端正文图片
> 自动下载展示，视频以“点击播放”方式预览。

## 目录结构

```
mind-weave-miniprogram/
├── app.js / app.json / app.wxss    # 应用入口与全局样式
├── config.js                       # 接口地址配置（生产默认 https://wei6130.top:8445/api）
├── utils/
│   ├── request.js                  # wx.request 封装（JWT、401/403 处理）
│   ├── format.js                   # 金额格式化、加项/扣项/公司合计/另发奖金分组
│   └── charts.js                   # 画布折线图/柱状图（无第三方依赖）
├── pages/
│   ├── login/                      # 登录页
│   ├── home/                       # 首页：区间统计 + 月度总薪资趋势 + 年度对比
│   ├── records/                    # 工资记录：年份/关键词/排序筛选，明细分组展开
│   ├── detail/                     # 工资详情：字段分组 + 批注
│   └── mine/                       # 我的：修改密码 / 退出登录
└── README-小程序开发部署说明.md
```

## 部署前必须完成的两件事

### 1. 域名 ICP 备案

微信要求小程序 request 合法域名**必须完成 ICP 备案**。请先确认 `wei6130.top` 已完成备案
（备案主体不限，个人备案即可）。未备案的域名无法配置为小程序服务器域名。

### 2. 配置服务器域名

登录[微信公众平台](https://mp.weixin.qq.com) → 开发 → 开发设置 → 服务器域名 →
在 **request 合法域名** 和 **downloadFile 合法域名** 中都添加：

```
https://wei6130.top:8445
```

说明（来自微信官方文档）：

- HTTPS 域名**可以配置端口**，配置后只能请求该地址（不能请求 `wei6130.top` 或其它端口）；
- 域名不能用 IP 地址或 localhost；
- 证书必须有效、受系统信任、域名匹配、TLS 1.2 及以上（当前证书已满足）。

> 注意：首页轮播图、文章正文图片需要下载展示，因此 **downloadFile 合法域名**
> 也必须配置 `https://wei6130.top:8445`，否则图片会加载失败（只显示占位图）。

## 本地开发调试（微信开发者工具）

1. 打开微信开发者工具 → 导入项目 → 选择本目录 `mind-weave-miniprogram/`；
2. 把 `project.config.json` 中的 `appid` 改为你的小程序 AppID
   （不填写 AppID 时可用游客模式，但无法上传发布）；
3. 调试时若接口域名尚未配置/证书未就绪，勾选
   「详情 → 本地设置 → 不校验合法域名、TLS 版本以及 HTTPS 证书」；
4. 如需临时指向其它后端，在调试器 Console 执行：
   `wx.setStorageSync('salary_api_base', 'http://127.0.0.1:8080/api')`，
   恢复默认：`wx.removeStorageSync('salary_api_base')`。

## 发布流程

1. 开发者工具右上角「上传」→ 填版本号与备注；
2. 公众平台 → 管理 → 版本管理 → 将上传的版本设为**体验版**，手机扫码真机测试；
3. 测试通过后点击「提交审核」（个人主体需在后台填写类目与服务内容声明）；
4. 审核通过后点击「发布」。

## 个人主体小程序注意事项

- 类目建议选择「工具-效率」或相近允许的类目，名称建议体现“知识管理/效率工具”；
- 需在公众平台填写《用户隐私保护指引》（本项目仅收集登录账号密码，不调用
  头像/位置等隐私接口，填写时如实声明即可）；
- 个人主体不支持微信支付，本项目也不依赖支付能力；
- 工资数据属敏感信息，建议只给内部人员使用，避免公开推广。

## 账号与安全

- 登录使用系统中已有的用户（如 `admin`），登录后自动切换为“首页”；
- 修改密码后需重新登录；
- 登录态使用 JWT，默认 24 小时有效，过期后自动跳转登录页。

## 后续可扩展（需后端配合）

- **微信一键登录**：后端新增 `wx.login` 的 code2session 接口（AppID/AppSecret 保存在服务端），
  小程序首次用账号密码绑定 openid，之后免密登录；
- **消息订阅/工资条推送**：个人主体小程序可用订阅消息，需用户主动订阅；
- **更多业务模块**：直接在本工程中新增 `pages/xxx` 并在 `app.json` 注册即可。
